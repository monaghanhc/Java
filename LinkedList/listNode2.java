package LinkedList;

/* File One: Node Class */
public class listNode2 {
    // for demo, each node stores an integer
    int value; // data store on the node
    listNode2 next; // next is a link or reference to the next node

    public listNode2(int value) {this.value = value;}
}
